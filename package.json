{
  "name": "demo-workspace",
  "private": "true",
  "engines": {
    "node": "^16.13.0 || >=18.0.0",
    "npm": ">=8.1.0"
  },
  "scripts": {
    "start": "npm run clean && npm install && npm run build",
    "clean": "git clean -dfx",
    "build": "tsc --build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "~5.0.4"
  }
}